# -*- coding:utf-8 -*-

"""The implementation of the verification algorithm contains a cycle graph using DFS."""


def contains_circle(graph):
    """Return only false.

    Args:
        graph: graph for check

    Returns:
        result (bool): false if graph not contains circle
    """
    return False
